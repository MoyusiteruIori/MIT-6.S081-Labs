a0-a7 
a2 
没有对应的调用，这两个函数被内联了。 
0x0000000000000628, main 中使用 pc 相对寻址来计算得到这个地址。 
0x0000000000000038, `jalr` 指令的下一条汇编指令的地址。 
"He110 World";  
0x726c6400;  
不需要，57616 的十六进制是 110，无论端序（十六进制和内存中的表示不是同个概念） 
输出的是一个受调用前的代码影响的“随机”的值。 
因为 `printf` 尝试读的参数数量比提供的参数数量多。 
第二个参数 `3` 通过 a1 传递，而第三个参数对应的寄存器 a2 在调用前不会被设置为任何具体的值，而是会 包含调用发生前的任何已经在里面的值。